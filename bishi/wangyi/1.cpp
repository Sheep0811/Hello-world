#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        vector<vector<int>> a(3,vector<int>(3,0));
        set<int>a1;
        set<int>a2;
        set<int>a3;
        char tmp;
        for(int i=0;i<9;++i)
        {
            cin>>tmp;
            if(tmp!='*')
        } 
    }
    system("pause");
    return 0;
}
