#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main()
{
    string s;
    getlin(cin,s);
    vector<string> str;
    int n=s.size();
    for(int i=0;i<n;++i)
    {
        str.push_back(s.substr(i));
    }
    sort(str.begin(),str.end());
    int maxlen=0;
    for(int i=0;i<str.size()-1;++i)
    {
        int len=get
    }
    system("pause"); 
    return 0;
}